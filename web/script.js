// script.js\nconst chatBody = document.getElementById('chat-body');\nconst messageInput = document.getElementById('message-input');\nconst sendButton = document.getElementById('send-button');\n\nlet messages = [];\n\n// Função para enviar mensagem\nfunction sendMessage(message) {\n    // Aqui você pode implementar a lógica para enviar a mensagem para o servidor\n    console.log(`Mensagem enviada: ${message}`);\n    // Adiciona a mensagem à lista de mensagens\n    messages.push(message);\n    // Atualiza a exibição das mensagens\n    updateChat();\n}\n\n// Função para atualizar a exibição das mensagens\nfunction updateChat() {\n    chatBody.innerHTML = '';\n    messages.forEach((message) =\u003e {\n        const messageElement = document.createElement('div');\n        messageElement.textContent = message;\n        chatBody.appendChild(messageElement);\n    });\n}\n\n// Adiciona evento de click ao botão de enviar\nsendButton.addEventListener('click', () =\u003e {\n    const message = messageInput.value.trim();\n    if (message !== '') {\n        sendMessage(message);\n        messageInput.value = '';\n    }\n});